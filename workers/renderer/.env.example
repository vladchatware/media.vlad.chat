# Copy to workers/renderer/.env on worker machine.
# Shared secret; the app must send it as `Authorization: Bearer <secret>`
# (workflows/render.ts sends it when RENDERER_SECRET is set on the app).
RENDERER_SECRET=replace-with-a-shared-secret

# Renderer uploads completed files directly and consumes Vercel Queue from
# outside Vercel. Use the same project credentials in the app deployment so
# publisher and consumer tokens have the same queue environment scope.
BLOB_READ_WRITE_TOKEN=replace-with-blob-token
VERCEL_API_TOKEN=replace-with-scoped-vercel-token
VERCEL_PROJECT_ID=prj_replace_me
# VERCEL_ORG_ID=team_replace_me
VERCEL_QUEUE_REGION=iad1
VERCEL_RENDER_QUEUE_TOPIC=media-render
VERCEL_RENDER_QUEUE_CONSUMER_GROUP=media-renderer
RENDER_WORKER_CONCURRENCY=1
RENDER_MAX_DELIVERY_COUNT=5

# Recommended for reliable full-length SoundCloud HLS resolution. Without
# these, renderer falls back to music.vlad.chat's public stream endpoint.
ANALYSIS_SERVICE_SECRET=replace-with-analysis-service-secret
CONVEX_SITE_URL=https://replace-with-your-convex-site
# SOUNDCLOUD_USER_ID=

# Optional: pin a cloudflared image.
# CLOUDFLARED_IMAGE=cloudflare/cloudflared:latest
